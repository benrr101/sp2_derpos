atoi() - DERP OS man page

Name -------------------
atoi() - converts a string into an integer.

Synopsis ---------------

#include "string.h"

void atoi( const char *s );

Description ------------

The atoi() function converts the initial portion of the string pointed to by
nptr to int. The behavior is the same as

    strtol(nptr, (char **) NULL, 10); 

except that atoi() does not detect errors.

The atol() and atoll() functions behave the same as atoi(), except that they
convert the initial portion of the string to their return type of long or
long long. atoq() is an obsolete name for atoll(). 

s	-	The string to process into its repsective integer form.

Return Value -----------

The converted value

Source -----------------

http://linux.die.net/man/3/atoi
